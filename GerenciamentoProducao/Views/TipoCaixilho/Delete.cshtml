@model GerenciamentoProducao.Models.TipoCaixilho

@{
    ViewData["Title"] = "Excluir Tipo de Caixilho";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="h3 mb-0 text-white">Excluir Tipo de Caixilho</h1>
            <p class="text-white-50">Revise as informações abaixo antes de confirmar a exclusão.</p>
        </div>
    </div>

    <div class="row align-items-stretch g-4">
        <div class="col-lg-7 col-xl-6">
            <div class="card shadow border-0 h-100">
                <div class="card-header bg-danger text-white d-flex justify-content-between align-items-center">
                    <h6 class="m-0">Tem certeza que deseja excluir este tipo de caixilho?</h6>
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning d-flex align-items-center" role="alert">
                        <i class="fas fa-info-circle me-2"></i>
                        <div>Essa ação é permanente e pode impactar cadastros que utilizam este tipo de caixilho.</div>
                    </div>

                    <dl class="row mb-0">
                        <dt class="col-sm-4 text-muted">Identificador</dt>
                        <dd class="col-sm-8">
                            <span class="badge bg-secondary">@Model.IdTipoCaixilho</span>
                        </dd>

                        <dt class="col-sm-4 text-muted">Descrição</dt>
                        <dd class="col-sm-8 fw-semibold">@Model.DescricaoCaixilho</dd>
                    </dl>
                </div>
                <div class="card-footer">
                    <form asp-action="Delete" class="d-flex justify-content-between align-items-center">
                        <a asp-action="Index" class="btn btn-outline-secondary">
                            <i class="fas fa-arrow-left"></i> Cancelar
                        </a>
                        <div>
                            <input type="hidden" asp-for="IdTipoCaixilho" />
                            <button type="submit" class="btn btn-danger">
                                <i class="fas fa-trash"></i> Excluir
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-5 col-xl-4">
            <div class="card shadow border-0 h-100">
                <div class="card-header bg-secondary text-white">
                    <h6 class="m-0">Antes de continuar</h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled mb-0">
                        <li class="mb-3 d-flex align-items-start">
                            <span class="badge bg-danger me-3"><i class="fas fa-link"></i></span>
                            <span>Verifique se não existem cadastros de caixilhos vinculados a este tipo.</span>
                        </li>
                        <li class="mb-3 d-flex align-items-start">
                            <span class="badge bg-primary me-3"><i class="fas fa-edit"></i></span>
                            <span>Considere atualizar o cadastro caso apenas precise ajustar a descrição.</span>
                        </li>
                        <li class="d-flex align-items-start">
                            <span class="badge bg-dark me-3"><i class="fas fa-undo"></i></span>
                            <span>Após a exclusão, será necessário recriar o tipo caso seja necessário novamente.</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
