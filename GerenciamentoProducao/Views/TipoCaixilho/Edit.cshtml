@model GerenciamentoProducao.Models.TipoCaixilho

@{
    ViewData["Title"] = "Editar Tipo de Caixilho";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="h3 mb-0 ">Editar Tipo de Caixilho</h1>
            <p class="">Atualize as informações necessárias e salve as alterações.</p>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-7 col-xl-6">
            <div class="card shadow border-0 h-100">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">Dados do Tipo de Caixilho</h6>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" asp-route-id="@Model.IdTipoCaixilho" method="post">
                        <div asp-validation-summary="All" class="alert alert-danger" role="alert"></div>
                        <input type="hidden" asp-for="IdTipoCaixilho" />

                        <div class="mb-3">
                            <label asp-for="DescricaoCaixilho" class="form-label">Descrição</label>
                            <input asp-for="DescricaoCaixilho" class="form-control" placeholder="Ex.: Janela de correr" />
                            <span asp-validation-for="DescricaoCaixilho" class="text-danger"></span>
                        </div>

                        <div class="d-flex justify-content-between">
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left"></i> Cancelar
                            </a>
                            <input type="submit" value="Salvar Alterações" class="btn btn-primary" />

                        </div>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-5 col-xl-4">
            <div class="card shadow border-0 h-100">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">Dicas de preenchimento</h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled mb-0">
                        <li class="mb-3 d-flex">
                            <span class="badge bg-info me-3"><i class="fas fa-font"></i></span>
                            <div>
                                Utilize uma descrição objetiva que ajude a identificar o tipo de caixilho durante o cadastro de famílias e caixilhos.
                            </div>
                        </li>
                        <li class="mb-3 d-flex">
                            <span class="badge bg-success me-3"><i class="fas fa-check"></i></span>
                            <div>
                                Verifique se não existe outro cadastro com a mesma descrição para evitar duplicidades.
                            </div>
                        </li>
                        <li class="d-flex">
                            <span class="badge bg-warning text-dark me-3"><i class="fas fa-users-cog"></i></span>
                            <div>
                                Alterações serão refletidas nas telas que utilizam este tipo de caixilho imediatamente após salvar.
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
@* 
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
} *@
