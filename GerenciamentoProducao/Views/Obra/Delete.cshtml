@model GerenciamentoProducao.Models.Obra

@{
    ViewData["Title"] = "Delete";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <h1 class="h3 mb-0 text-white">Excluir Obra</h1>
            <p class="text-white-50">Confira as informações antes de confirmar a exclusão.</p>
        </div>
    </div>

    <div class="row">
        <div class="col-lg-8">
            <div class="card shadow border-0">
                <div class="card-header bg-danger text-white d-flex justify-content-between align-items-center">
                    <h6 class="m-0">Tem certeza que deseja excluir esta obra?</h6>
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <div class="card-body">
                    <div class="alert alert-warning d-flex align-items-center" role="alert">
                        <i class="fas fa-info-circle me-2"></i>
                        <div>
                            Esta ação não poderá ser desfeita e removerá também o evento relacionado no calendário, se existir.
                        </div>
                    </div>

                    @if (!string.IsNullOrEmpty(Model.ImagemObraPath))
                    {
                        <div class="text-center mb-4">
                            <img src="@Url.Content("~/" + Model.ImagemObraPath)" alt="Imagem da obra" class="img-fluid rounded shadow-sm" style="max-height: 240px; object-fit: cover;" />
                        </div>
                    }

                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="p-3 rounded bg-light border">
                                <h6 class="text-primary text-uppercase small mb-2">Identificação</h6>
                                <p class="mb-1"><strong>Nome:</strong> @Model.Nome</p>
                                <p class="mb-1"><strong>Construtora:</strong> @Model.Construtora</p>
                                <p class="mb-1"><strong>CNPJ:</strong> @Model.Cnpj</p>
                                <p class="mb-0"><strong>Responsável:</strong> @(Model.Usuario?.NomeUsuario ?? "Não informado")</p>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="p-3 rounded bg-light border">
                                <h6 class="text-primary text-uppercase small mb-2">Localização</h6>
                                <p class="mb-1"><strong>Logradouro:</strong> @Model.Logradouro, Nº @Model.Nro</p>
                                <p class="mb-1"><strong>Bairro:</strong> @Model.Bairro</p>
                                <p class="mb-0"><strong>CEP / UF:</strong> @Model.Cep - @Model.Uf</p>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="p-3 rounded bg-light border">
                                <h6 class="text-primary text-uppercase small mb-2">Status</h6>
                                <p class="mb-1">
                                    <strong>Status:</strong>
                                    <span class="badge text-bg-@(Model.StatusObra == "Concluída" ? "success" : Model.StatusObra == "Em Andamento" ? "primary" : Model.StatusObra == "Atrasada" ? "danger" : "secondary")">@Model.StatusObra</span>
                                </p>
                                <p class="mb-1"><strong>Prioridade:</strong> <span class="badge bg-dark">@Model.Prioridade</span></p>
                                <p class="mb-0"><strong>Bandeira:</strong> <span class="badge badge-@(Model.Bandeira == "Verde" ? "success" : Model.Bandeira == "Amarela" ? "warning" : Model.Bandeira == "Vermelha" ? "danger" : "dark")">@Model.Bandeira</span></p>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="p-3 rounded bg-light border">
                                <h6 class="text-primary text-uppercase small mb-2">Peso</h6>
                                <p class="mb-1"><strong>Peso Total:</strong> @Model.PesoFinal.ToString("F0") kg</p>
                                <p class="mb-1"><strong>Peso Produzido:</strong> @Model.PesoProduzido.ToString("F0") kg</p>
                                <p class="mb-0"><strong>Conclusão:</strong> @Model.PercentualConclusao.ToString("F1")%</p>
                            </div>
                        </div>
                        @if (!string.IsNullOrEmpty(Model.Observacoes))
                        {
                            <div class="col-12">
                                <div class="p-3 rounded bg-light border">
                                    <h6 class="text-primary text-uppercase small mb-2">Observações</h6>
                                    <p class="mb-0 text-muted">@Model.Observacoes</p>
                                </div>
                            </div>
                        }
                    </div>
                </div>
                <div class="card-footer d-flex justify-content-between">
                    <a asp-action="Index" class="btn btn-outline-secondary">
                        <i class="fas fa-arrow-left"></i> Cancelar
                    </a>
                    <form asp-action="Delete" class="d-inline">
                        <input type="hidden" asp-for="IdObra" />
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-trash"></i> Excluir Obra
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card shadow border-0">
                <div class="card-header bg-secondary text-white">
                    <h6 class="m-0">Resumo Rápido</h6>
                </div>
                <div class="card-body">
                    <ul class="list-unstyled mb-0">
                        <li class="mb-3 d-flex align-items-center">
                            <span class="badge bg-danger me-2"><i class="fas fa-exclamation-circle"></i></span>
                            <span>Certifique-se de que todos os dados já foram migrados ou arquivados.</span>
                        </li>
                        <li class="mb-3 d-flex align-items-center">
                            <span class="badge bg-primary me-2"><i class="fas fa-calendar-times"></i></span>
                            <span>Eventos associados no Google Calendar serão removidos automaticamente.</span>
                        </li>
                        <li class="d-flex align-items-center">
                            <span class="badge bg-dark me-2"><i class="fas fa-images"></i></span>
                            <span>A imagem da obra será excluída do servidor.</span>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>
