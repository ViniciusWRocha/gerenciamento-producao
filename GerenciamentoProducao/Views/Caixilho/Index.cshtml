@model IEnumerable<GerenciamentoProducao.Models.Caixilho>

@{
    ViewData["Title"] = "Caixilhos";
}

<div class="container-fluid">
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center">
                <div>
                    <h1 class="h3 mb-0">
                        <i class="fas fa-window-maximize"></i> Gerenciamento de Caixilhos
                    </h1>
                    <p class="mb-0">Controle completo de caixilhos e esquadrias de alumínio</p>
                </div>
                <div class="text-right">
                    <small class="text-muted">
                        <i class="fas fa-info-circle"></i> 
                        Total: @Model.Count() caixilhos cadastrados
                    </small>
                </div>
            </div>
        </div>
    </div>

    <div class="row mb-4">
        <div class="col-12">
            <div class="card shadow">
                <div class="card-header py-3 d-flex justify-content-between align-items-center">
                    <div>
                        <h6 class="m-0 font-weight-bold text-primary">
                            <i class="fas fa-list"></i> Lista de Caixilhos
                        </h6>
                        <small class="text-muted">Visualize e gerencie todos os caixilhos cadastrados</small>
                    </div>
                    <div>
                        <a asp-action="Liberacoes" class="btn btn-warning btn-sm me-2">
                            <i class="fas fa-unlock"></i> Liberações
                        </a>
                        <a asp-action="Create" class="btn btn-primary btn-sm">
                            <i class="fas fa-plus"></i> Novo Caixilho
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-bordered" width="100%" cellspacing="0">
                            <thead>
                                <tr>
                                    <th>Nome</th>
                                    <th>Dimensões</th>
                                    <th>Quantidade</th>
                                    <th>Peso</th>
                                    <th>Obra</th>
                                    <th>Família</th>
                                    <th>Status</th>
                                    <th>Liberação</th>
                                    <th>Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td>
                                            <strong>@item.NomeCaixilho</strong>
                                            @if (!string.IsNullOrEmpty(item.Observacoes))
                                            {
                                                <br><small class="text-muted">@item.Observacoes</small>
                                            }
                                        </td>
                                        <td>
                                            <small>
                                                <strong>L:</strong> @item.Largura cm<br>
                                                <strong>A:</strong> @item.Altura cm
                                            </small>
                                        </td>
                                        <td>@item.Quantidade</td>
                                        <td>
                                            <small>
                                                <strong>Unit:</strong> @item.PesoUnitario.ToString("F1") kg<br>
                                                <strong>Total:</strong> @((item.PesoUnitario * item.Quantidade).ToString("F1")) kg
                                            </small>
                                        </td>
                                        <td>
                                            <small>@(item.Obra?.Nome ?? "N/A")</small>
                                        </td>
                                        <td>
                                            <small>@(item.FamiliaCaixilho?.DescricaoFamilia ?? "N/A")</small>
                                        </td>
                                        <td>
                                            <span class="badge text-bg-@(item.StatusProducao == "Liberado" ? "success" : item.StatusProducao == "Pendente" ? "warning" : "secondary")">
                                                @item.StatusProducao
                                            </span>
                                        </td>
                                        <td>
                                            @if (item.Liberado)
                                            {
                                                <span class="badge badge-success text-bg-success">
                                                    <i class="fas fa-check"></i> Liberado
                                                </span>
                                                @if (item.DataLiberacao.HasValue)
                                                {
                                                    <br><small class="text-muted">@item.DataLiberacao.Value.ToString("dd/MM/yyyy")</small>
                                                }
                                            }
                                            else
                                            {
                                                <span class="badge badge-warning text-bg-warning">
                                                    <i class="fas fa-clock"></i> Pendente
                                                </span>
                                            }
                                        </td>
                                        <td>
                                            <div class="btn-group" role="group">
                                                <a asp-action="Edit" asp-route-id="@item.IdCaixilho" class="btn btn-sm btn-warning">
                                                    <i class="fas fa-edit"></i>
                                                </a>
                                                <a asp-action="Details" asp-route-id="@item.IdCaixilho" class="btn btn-sm btn-info">
                                                    <i class="fas fa-eye"></i>
                                                </a>
                                                <a asp-action="Delete" asp-route-id="@item.IdCaixilho" class="btn btn-sm btn-danger">
                                                    <i class="fas fa-trash"></i>
                                                </a>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>